r"""
The escape sequence characters are special characters that are used to represent certain characters or actions in a string. 
They are typically preceded by a backslash (\) and are used to represent characters that cannot be easily typed or displayed, 
such as newlines, tabs, or special symbols.
Some of the most common escape sequence characters are:
\n - represents a newline character, which moves the cursor to the next line.
\t - represents a tab character, which adds a horizontal space to the text.
\\ - represents a backslash character, which is used to escape other characters.
\' - represents a single quote character, which is used to escape single quotes in a string.
\" - represents a double quote character, which is used to escape double quotes in a string.
\b - represents a backspace character, which moves the cursor back one space.
\r - represents a carriage return character, which moves the cursor to the beginning of the line
"""

################################\n use #################################################################################

print("The entire section is written on one print statement \n You see this statement is using  the \\n method ")

################################\t use##################################################################################

print("\nThe \t is used in this r\tspace\tthis is the tab  space added by \t \n")

###############################\\use #####################################################################################

print("We can use double backslashh to print this ==> \\ \n")

###################################\' or \" use #####################################################################################
print("We can use single quote or double quote to print this ==> \' or \" ")

